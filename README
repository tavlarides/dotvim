This is my vim configuration files

To install clone from github

git clone git://github.com/tavlarides/dotvim.git ~/.vim


and create the symlinks:

ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.gvim/vimrc ~/.gvimrc

